#name
spring.application.name=mediaplatform

# --- DataSource (Neon PostgreSQL) ---
spring.datasource.url=jdbc:postgresql://ep-round-fog-a145028l-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_Wg5Cet8uVSAo
spring.datasource.driver-class-name=org.postgresql.Driver

# --- Hibernate / JPA ---
spring.jpa.database=postgresql

# create / update / validate (update recommended in dev)
spring.jpa.hibernate.ddl-auto=update  
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- JWT Configuration ---
app.jwt.secret=ReplaceWithASecretKeyOfAtLeast32Chars!

# 1 hour for auth tokens
app.jwt.expiration-ms=3600000         

# 10 minutes for stream tokens
app.jwt.stream-expiration-ms=600000    

# --- File Upload Config ---
app.file.upload-dir=D:\\Personal Project\\uploads

# --- (Optional) Logging ---
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
